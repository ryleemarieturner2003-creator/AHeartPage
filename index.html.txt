<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>A + R Glowing Heart</title>
  <style>
    html, body {
      margin: 0;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      background: radial-gradient(circle, #3a0000 0%, #120000 100%);
      overflow: hidden;
      font-family: "Inter", sans-serif;
    }

    /* Glow behind heart */
    .glow {
      position: absolute;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(255,0,0,0.8), rgba(255,0,0,0.25), transparent 80%);
      filter: blur(40px);
      animation: pulseGlow 3s ease-in-out infinite;
    }

    @keyframes pulseGlow {
      0% { transform: scale(1); opacity: 0.85; }
      50% { transform: scale(1.15); opacity: 1; }
      100% { transform: scale(1); opacity: 0.85; }
    }

    /* Heart container */
    .heart {
      width: 340px;
      height: 300px;
      position: relative;
      animation: beat 1.2s infinite ease-in-out;
    }

    @keyframes beat {
      0% { transform: scale(1); }
      50% { transform: scale(1.15); }
      100% { transform: scale(1); }
    }

    /* SVG heart */
    .heart svg {
      width: 100%;
      height: 100%;
      filter: drop-shadow(0 0 35px rgba(255, 30, 30, 1)) drop-shadow(0 0 60px rgba(255, 40, 40, 0.9));
    }

    /* Text inside heart */
    .inside-text {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 48px;
      font-weight: 700;
      color: white;
      text-shadow: 0 0 8px rgba(255, 80, 80, 0.9);
      pointer-events: none;
    }
  </style>
</head>
<body>

  <div class="glow"></div>

  <div class="heart">
    <svg viewBox="0 0 32 29" xmlns="http://www.w3.org/2000/svg">
      <path fill="#ff1a1a" d="M23.6 0C20.9 0 18.6 1.4 16 3.8 13.4 1.4 11.1 0 8.4 0 3.4 0 0 4.4 0 9.8c0 7.1 7.7 12 15.6 19.2.5.5 1.3.5 1.8 0C24.3 21.8 32 17 32 9.8 32 4.4 28.6 0 23.6 0z"/>
    </svg>
    <div class="inside-text">A + R</div>
  </div>

</body>
</html>
